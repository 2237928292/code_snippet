# vue 相关的代码片段 #

#### 赋值 #####

> 给某对象赋值时 ， 例如 obj 直接使用 . 赋值后不可更改 需使用 $set

```
this.$set(obj,'key',value)
```

#### 赋值 #####

> 给某对象赋值时 ， 例如 obj 直接使用 . 赋值后不可更改 需使用 $set

```
this.$set(obj,'key',value)
```

